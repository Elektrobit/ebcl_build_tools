[General]
unpack=true
bootstrap=PrimaryRepo
aptsources=PrimaryRepo

[PrimaryRepo]
packages=apt apt-utils base-files base-passwd
source={{ repo_url }}
keyring={{ repo_keyring_package }}
suite={{ distro }}

